{
  "entities": {
    "Quote": {
      "title": "Quote",
      "description": "Une citation philosophique",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "text": { "type": "string" },
        "author": { "type": "string" },
        "theme": { "type": "string" },
        "work": { "type": "string" }
      },
      "required": ["text", "author"]
    },
    "UserFavorite": {
      "title": "UserFavorite",
      "description": "Favoris d'un utilisateur",
      "type": "object",
      "properties": {
        "quoteId": { "type": "string" },
        "addedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["quoteId", "addedAt"]
    },
    "UserProfile": {
      "title": "UserProfile",
      "description": "Profil utilisateur avec informations personnelles et parrainage",
      "type": "object",
      "properties": {
        "username": { "type": "string" },
        "phoneNumber": { "type": "string" },
        "gender": { "type": "string", "enum": ["masculin", "féminin", "non-binaire"] },
        "acceptedTerms": { "type": "boolean" },
        "referralCode": { "type": "string", "description": "Code unique de parrainage de l'utilisateur" },
        "referredBy": { "type": "string", "description": "Code du parrain si applicable" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["username", "phoneNumber", "acceptedTerms", "gender", "referralCode"]
    }
  },
  "auth": {
    "providers": ["google.com", "password", "anonymous"]
  },
  "firestore": {
    "/quotes/{quoteId}": {
      "schema": "Quote",
      "description": "Collection globale de citations"
    },
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Profil de l'utilisateur"
    },
    "/users/{userId}/favorites/{favoriteId}": {
      "schema": "UserFavorite",
      "description": "Citations favorites de l'utilisateur"
    }
  }
}