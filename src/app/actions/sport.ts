
'use server';

/**
 * @fileOverview Oracle de Transmission Sportive - Désactivé.
 * La logique de récupération API a été supprimée conformément au décret de l'esprit.
 */

export async function getDailyMatches() {
  // Le flux API a été interrompu.
  return [];
}
