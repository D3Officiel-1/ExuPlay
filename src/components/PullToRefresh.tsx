"use client";

import React from "react";

/**
 * @fileOverview Ce composant a été désactivé. Il retourne simplement ses enfants
 * sans aucune logique de rafraîchissement personnalisée.
 */

export function PullToRefresh({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
