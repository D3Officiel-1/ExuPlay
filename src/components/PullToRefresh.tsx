"use client";

/**
 * @fileOverview Ce composant a été supprimé.
 * Le système de Pull-to-Refresh personnalisé n'est plus utilisé dans l'application.
 */
export function PullToRefresh({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
